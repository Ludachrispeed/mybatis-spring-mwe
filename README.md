MyBatis-Spring Minimal Example
==============================

Uses Java configuration for all beans, and XML for MyBatis mappers.

Has one service, "PeopleService", which can query an in-memory HSQLDB to
retrieve a "Person" by first name.